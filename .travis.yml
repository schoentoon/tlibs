language: c
compiler:
  - gcc
  - clang
script:
  - make clean
  - make CC=$CC
after_script:
  - make test
notifications:
  irc:
    channels:
      - "irc.freenode.org#schoentoon"
    on_success: always
    on_failure: always
